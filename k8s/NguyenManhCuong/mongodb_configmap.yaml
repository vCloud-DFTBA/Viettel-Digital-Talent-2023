apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-configmap
immutable: false
data:
  database-url: mongodb-service
  mongodb.conf: |
    storage:
      dbPath: /data/db
    replication:
        replSetName: "rs0"