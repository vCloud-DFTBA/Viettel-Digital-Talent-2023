---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: fastapi
  name: fastapi
  namespace: vdt
spec:  
  ports:
  - nodePort: 30800
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: fastapi
  type: NodePort
