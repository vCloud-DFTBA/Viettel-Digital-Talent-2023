events {}

http {
    upstream upstream-server {
        server {{ server_1 }}:8080;
        server {{ server_2 }}:8080;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://upstream-server;
        }

    }
}