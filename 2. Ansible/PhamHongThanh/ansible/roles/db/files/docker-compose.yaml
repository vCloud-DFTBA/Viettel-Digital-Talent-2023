version: '3.8'
services:
  mongodb:
    build: .
    container_name: database_mongo
    ports:
      - "27018:27017"
    networks:
      my_network:
        aliases:
          - mongodb
networks:
  my_network:
    external: 
      name: my_network