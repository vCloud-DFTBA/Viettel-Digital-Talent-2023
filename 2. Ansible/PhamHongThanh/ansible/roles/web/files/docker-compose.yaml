version: '3.8'
services:
  nginx:
    build: .
    container_name: frontend_nginx
    ports:
      - 8081:80
    networks:
      - my_network
networks:
  my_network:
    external:
      name: my_network