---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /home/imey/Desktop/VTS-Trainning/HW2/venv/bin/python
      ansible_become_user: root
      # ansible_become_password: "{{ root_pass }}" # add --ask-become-pass to show enter password
      hostname: vtn-012
    