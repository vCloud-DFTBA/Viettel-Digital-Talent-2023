version: "3.8"

services:
  frontend:
    image: frontend:latest
    build: ./frontend
    container_name: frontend
    ports:
      - 80:80
    networks:
      - app

networks:
  app:
    external: true
