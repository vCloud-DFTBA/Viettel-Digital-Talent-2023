events {}
http {
    include mime.types;
    sendfile on;
    server {
        listen 80;
        root /etc/static;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }

        location /api {
            proxy_pass http://api-service:1234;
        }
    }
}