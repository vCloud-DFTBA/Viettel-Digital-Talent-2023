apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: vtapp-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30011
  type: NodePort
