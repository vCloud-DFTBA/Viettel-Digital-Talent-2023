apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-backend.fullname" . }}-test-connection"
  labels:
    {{- include "helm-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
