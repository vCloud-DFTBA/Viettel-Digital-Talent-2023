---
all:
    hosts:
        localhost:
            ansible_connection: local
            ansible_python_interpreter: /home/rejk/Desktop/project/project3/venv/bin/python
            ansible_become_user: root
            ansible_become_password: "<Pass cua ban>"
            hostname: host
