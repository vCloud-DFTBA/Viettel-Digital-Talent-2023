apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  ports:
    - protocol: TCP
      port: 3030
      targetPort: 3030
      nodePort: 30332
  type: NodePort
  selector:
    app: frontend
