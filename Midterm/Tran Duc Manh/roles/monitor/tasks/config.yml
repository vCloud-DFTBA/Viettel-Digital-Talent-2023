---
- name: Create Monitor group info.
  group:
    name: monitor

- name: Create user.
  user:
    name: monitor
    group: monitor

- name: Check docker install.
  package:
    name: docker
    state: present