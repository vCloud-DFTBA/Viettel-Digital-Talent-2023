---
all:
  hosts:
    localhost:
      ansible_connection: local
      # ansible_python_interpreter: /home/chung/project_ansible/venv/bin/python
      ansible_become_user: root
      ansible_become_password: 1
